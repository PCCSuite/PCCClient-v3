import 'package:http/http.dart' as http;

Future<void> registerAttendance() async {
  // TODO: register attendance
  // http.Response response = await http.get(Uri.parse(serverInfo.attendanceURL),
  //     headers: {"Authorization": "Bearer ${await getToken()}"});
  // if (response.statusCode != 200) {
  //   throw Exception(
  //       "Failed to register attendance: ${response.statusCode}, body: ${response.body}");
  // }
}
